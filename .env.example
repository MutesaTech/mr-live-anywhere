# Web3Forms access key — used to email channel reports to your inbox.
# It is safe to expose in client-side code (it is an alias to your email, not a secret key).
# Get one at https://web3forms.com (create a form with your email, verify it, copy the key).
VITE_WEB3FORMS_ACCESS_KEY=
